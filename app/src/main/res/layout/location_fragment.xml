<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <Spinner
        android:id="@+id/spinner_location_group"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:hint="Unesite pojam:" />

    <Button
        android:id="@+id/location_button_search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:layout_marginRight="5dp"
        android:text="Search"
        android:layout_alignParentRight="true"
        android:onClick="location_search_pressed"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvLocationList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/location_button_search"
        />
</RelativeLayout>